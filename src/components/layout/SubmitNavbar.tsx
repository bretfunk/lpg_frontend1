import * as React from "react";

export const SubmitNavbar = (props: any) => (
  <nav className="navbar fixed-bottom navbar-light bg-primary justify-content-end">
    <button className="btn btn-danger right-align">Submit Form</button>
  </nav>
);
